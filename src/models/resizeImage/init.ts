import {clearImage, image$, marks$, originalHeight$, originalWidth$} from "./index";

image$.reset(clearImage);
originalWidth$.reset(clearImage);
originalHeight$.reset(clearImage);
marks$.reset(clearImage);
